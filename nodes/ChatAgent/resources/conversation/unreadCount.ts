import type { INodeProperties } from 'n8n-workflow';

// Unread Count takes no parameters — its field list exists only to keep the
// one-description-file-per-operation pattern (see lat.md/nodes.md).
export const conversationUnreadCountDescription: INodeProperties[] = [];